version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: bamachat-ollama
    ports:
      - "11435:11434"
    environment:
      - OLLAMA_NUM_GPU=0
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

volumes:
  ollama_data:
